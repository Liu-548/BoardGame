# CLAUDE.md

Đọc file này trước khi làm bất cứ việc gì trong repo.

## Bối cảnh

Game bài online kiểu Bang! (bản tự cài đặt lại luật, không dùng tài sản gốc), cho một nhóm bạn nhỏ chơi với nhau. Phi thương mại.

Chủ dự án **mới học lập trình, kiến thức web ở mức dưới căn bản**. Mục tiêu song song là học, không chỉ là có sản phẩm chạy được. Điều này quyết định cách bạn làm việc — xem phần "Cách làm việc" bên dưới.

## Ngăn xếp công nghệ — đã chốt, không đề xuất thay đổi

| Hạng mục | Lựa chọn |
|---|---|
| Ngôn ngữ | TypeScript |
| Server | Cloudflare Workers + Durable Objects |
| Framework | **Không dùng framework nào** — không React, Vue, Svelte, boardgame.io, Colyseus, PartyServer, Express, Socket.IO |
| Client | TypeScript + DOM thuần |
| Build | Vite (chỉ để bundle) + Wrangler |
| Test | Vitest |
| Dependencies | Càng ít càng tốt. **Hỏi trước khi thêm bất kỳ package nào.** |

## Cấu trúc thư mục

> **Đổi cấu trúc (2026-09-08):** ban đầu dự án chỉ có 1 game (Bang!) nên
> `src/core`/`src/server`/`src/client` nằm thẳng dưới `src/`. Từ khi bắt đầu
> làm màn "Sảnh" (chọn 1 trong nhiều game) và để dành chỗ cho Mạt Chược/Coup
> (2 game MỚI, hiện CHỈ LÀ PLACEHOLDER — chưa có code luật/state thật), toàn bộ
> code Bang! dời vào `src/games/bang/` để mỗi game có 1 thư mục riêng, không
> lẫn vào nhau. **Quy tắc bất di bất dịch bên dưới áp dụng y hệt, chỉ đổi vị
> trí** — vd quy tắc 1 giờ đọc là "`src/games/bang/core/` không được import gì
> từ `src/games/bang/server/`, `src/games/bang/client/`...".

```
src/
  hub/             ← MỚI: màn hình đầu tiên khi mở link — chọn 1 trong các
                      game (Bang!/Mạt Chược/Coup), CHƯA vào thẳng ván Bang!
                      như trước. Xem docs/roadmap/LO-TRINH.md.
  games/
    bang/
      core/          ← luật chơi. THUẦN. Không biết gì về mạng, DOM, Cloudflare.
        types.ts       ← kiểu dữ liệu state, action, card
        cards.ts       ← dữ liệu bộ bài (dữ liệu, không phải logic)
        rng.ts         ← sinh số ngẫu nhiên có seed
        setup.ts       ← tạo ván mới
        reduce.ts      ← reduce(state, action) => state mới (KHÔNG có pending.ts
                          riêng như dự tính ban đầu — stack pending quản lý ngay
                          trong reduce.ts, không tách file)
        view.ts        ← viewFor(state, playerId) => state đã lọc
        distance.ts    ← khoảng cách & tầm bắn
        equipment.ts   ← gắn/chuyển lá trang bị (Dynamite...)
        win.ts         ← điều kiện thắng theo phe
        deck.ts        ← drawTopCard() — tách khỏi reduce.ts để characters.ts
                          dùng được mà không vòng lặp import
        characters.ts  ← hệ thống hook + registry nhân vật (CHARACTERS — đủ
                          16/16 nhân vật gốc + 15 Dodge City + *ex, xem trạng
                          thái bên dưới)
        events.ts      ← lá sự kiện High Noon/A Fistful of Cards
      server/
        index.ts     ← Worker entry, định tuyến theo mã phòng
        room.ts      ← lớp Durable Object, 1 instance = 1 phòng
      client/
        net.ts       ← WebSocket, reconnect
        ui.ts        ← vẽ DOM
        main.ts
      protocol.ts    ← kiểu dữ liệu message client↔server (ClientMessage/
                        ServerMessage). Cả server/ và client/ đều cần đọc kiểu
                        này nên không đặt trong core/ (core/ vẫn không import
                        gì từ đây) hay riêng client/server — coi như "ngôn
                        ngữ chung" 2 bên.
    mahjong/         ← MỚI, CHỈ LÀ PLACEHOLDER — chưa có code thật, xem README.md
    coup/            ← MỚI, CHỈ LÀ PLACEHOLDER — chưa có code thật, xem README.md
test/
  core/                ← test cơ chế nền (reduce/pending/view/setup/rng/
                          distance/equipment/dynamite/jail...)
  characters/          ← test 16 nhân vật gốc
  dodge-city/          ← test mở rộng Dodge City
  high-noon/           ← test mở rộng High Noon (+ hạ tầng chồng sự kiện)
  a-fistful-of-cards/  ← test mở rộng A Fistful of Cards
  custom-characters/   ← test nhân vật *ex (bộ mở rộng "custom_characters")
index.html       ← trang gốc, Vite mặc định tìm ở đây (không phải public/,
                   xem ghi chú việc 2.1 bên dưới)
public/          ← tài sản tĩnh copy y nguyên (ảnh lá bài từ việc 4.6...),
                   style.css tạm để đây
docs/            ← MỚI: file .txt/.md đặc tả luật/thiết kế (chủ dự án viết),
                   dời ra khỏi gốc dự án cho gọn
  roadmap/         LO-TRINH.md
  bang-rules/      LUAT-BANG-CO-BAN.txt, NHAN-VAT-BANG-CO-BAN.txt,
                    House_Rule.txt, Luat_Bang_Mo_Rong_*.txt
  ui-ux/           GIAO-DIEN-UI-UX.txt
wrangler.jsonc
```

> CLAUDE.md và CHANGELOG.md vẫn ở gốc dự án (quy ước chuẩn, công cụ hay tìm ở đó).

> **Lệch 1 chỗ so với bản gốc file này:** dự tính ban đầu là `public/index.html`,
> nhưng Vite mặc định coi `index.html` ở gốc dự án là trang vào, còn `public/`
> chỉ dùng để copy y nguyên tài sản tĩnh (ảnh, v.v.) — đặt `index.html` vào
> `public/` khiến việc import file TypeScript từ `src/games/bang/client/` bị
> lỗi đường dẫn lúc `vite dev`. Đổi lại cho khớp quy ước của Vite (việc 2.1).
> Không cần file `vite.config.ts`/`vitest.config.ts` nào — mặc định của Vite
> đã đủ cho cả 2.

## Quy tắc bất di bất dịch

| # | Quy tắc | Lý do |
|---|---|---|
| 1 | `core/` **không được import** bất cứ thứ gì từ `server/`, `client/`, thư viện mạng, DOM, hay Cloudflare | Để test được, để sau này thêm board game khác dùng chung `server/` |
| 2 | Trong `core/` cấm `Math.random()`, `Date.now()`, `crypto.randomUUID()` | Ngẫu nhiên phải đi qua RNG có seed truyền vào. Cùng seed + cùng action log phải cho ra cùng kết quả, luôn luôn |
| 3 | State phải là dữ liệu JSON thuần | Phải serialize được để lưu, gửi, replay. Không class có method, không `Map`, không `Set`, không hàm trong state |
| 4 | **Không bao giờ `await` để chờ người chơi trả lời** | Xem "Mô hình chờ" bên dưới. Đây là quy tắc quan trọng nhất trong file này |
| 5 | Việc đang chờ lưu trong `state.pending`, là **mảng dùng như stack**, luôn xử lý phần tử **cuối cùng** | Reaction của Bang! lồng nhau nhiều tầng |
| 6 | Client **không bao giờ** nhận state đầy đủ. Server chỉ gửi `viewFor(state, playerId)` | Chống gian lận bằng DevTools. Không có ngoại lệ, kể cả khi debug |
| 7 | Trong Durable Object phải dùng `ctx.acceptWebSocket(ws)` và các handler `webSocketMessage` / `webSocketClose` / `webSocketError` | Dùng `ws.accept()` sẽ bị tính duration toàn bộ thời gian kết nối → 1 phòng sống 24h ≈ 10.800 GB-s/ngày, hạn mức miễn phí chỉ 13.000 |
| 8 | Cấm `setInterval` và `setTimeout` trong Durable Object. Cần hẹn giờ thì dùng `ctx.storage.setAlarm()` | Timer đang chờ sẽ chặn hibernate vĩnh viễn |
| 9 | Không để `fetch()` đang await treo lơ lửng trong DO | Cũng chặn hibernate |
| 10 | Mọi thay đổi trong `core/` phải kèm test | Chủ dự án chưa đủ kinh nghiệm để nhìn code mà phát hiện luật sai |

## Mô hình chờ (quy tắc 4 và 5)

**SAI:**

```ts
async function playBang(state, attacker, target) {
  const reply = await askPlayer(target, "Missed?");  // ❌ TUYỆT ĐỐI KHÔNG
  if (!reply) target.hp -= 1;
}
```

**ĐÚNG:** ghi việc đang chờ vào state rồi kết thúc hàm ngay.

```ts
// reduce trả về state mới, hàm kết thúc, server rảnh và có thể hibernate
state.pending.push({
  kind: "NEED_MISSED",
  player: "B",
  source: { card: "bang", from: "A" }
});
```

Khi B gửi hành động lên, nó đi qua `reduce` như mọi action khác: đọc `pending[pending.length - 1]`, kiểm tra hợp lệ, `pop()`, áp dụng hậu quả, và có thể `push()` thêm mục mới.

**Tại sao phải là stack:** A đánh Gatling → push yêu cầu Missed! cho B, C, D → C dùng Barrel → Barrel cần lật bài kiểm tra → push "draw check" lên **đỉnh** → giải quyết xong mới quay lại C → rồi mới tới D. Cái phát sinh sau phải xử lý trước.

## Chữ ký hàm cốt lõi — giữ nguyên hình dạng này

```ts
type Result = { state: GameState; events: GameEvent[] };

function reduce(state: GameState, action: Action): Result;
function viewFor(state: GameState, playerId: string): PlayerView;
function setupGame(playerIds: string[], seed: number, options: RuleOptions): GameState;
```

`reduce` phải thuần: cùng đầu vào → cùng đầu ra, không side effect, không sửa `state` truyền vào.

## Cách làm việc — quan trọng

| Loại việc | Bạn làm thế nào |
|---|---|
| Hạ tầng: wrangler config, WebSocket, build, CSS, HTML, boilerplate | Cứ viết. Giải thích ngắn gọn kết quả là đủ |
| `core/` — luật chơi | **Giải thích cách tiếp cận TRƯỚC, chờ đồng ý, rồi mới viết.** Đây là phần chủ dự án cần tự hiểu |
| Thêm dependency mới | Hỏi trước, kèm lý do tại sao không tự viết được |
| Đổi kiến trúc, đổi kiểu dữ liệu state | Hỏi trước |
| Luật Bang! không rõ ràng | **Dừng lại và hỏi.** Không tự đoán rồi cài đặt |

Nguyên tắc chung:

- **Mỗi lần một việc.** Không tự động làm thêm những thứ chưa được yêu cầu.
- **Không refactor toàn dự án** trừ khi được yêu cầu rõ ràng.
- Ưu tiên code dễ đọc hơn code ngắn. Chủ dự án phải đọc hiểu được.
- Đặt tên biến bằng tiếng Anh, comment bằng tiếng Việt.
- Khi giải thích, giả định người đọc **chưa biết** thuật ngữ web. Gặp từ mới thì giải thích ngắn ngay tại chỗ.
- Nếu thấy chủ dự án đang yêu cầu một thứ vi phạm quy tắc ở trên, **nói ra**, đừng lặng lẽ làm theo.

## Trạng thái hiện tại

> Cập nhật dòng này mỗi khi xong một giai đoạn. Xem `docs/roadmap/LO-TRINH.md`.

**Đang ở:** Giai đoạn 4 xong (4.1-4.6 khung, còn thiếu ảnh thật). Giai đoạn 5 — đủ 16/16 nhân vật (`core/characters.ts`), cơ chế "phát 2 lá nhân vật, chọn giữ 1" ĐÃ BẬT thật cho cả hotseat lẫn qua mạng (kèm đồng hồ 30 giây thật). Bia (Beer) đã hoàn thiện (ngoại lệ 2 người + hồi sinh tự động). **Cả 3 biến thể số người chơi (2/3/8 người) đều đã xong** — xem 3 mục changelog "Biến thể số người chơi" trong CHANGELOG.md. Xem `docs/bang-rules/NHAN-VAT-BANG-CO-BAN.txt` (đặc tả đủ 16 nhân vật + 9 loại hook, chủ dự án viết). **Đang làm lại giao diện theo `docs/ui-ux/GIAO-DIEN-UI-UX.txt`** (đặc tả 12 mục, chủ dự án viết) — đợt 1 (màu chrome, bàn tròn qua mạng, trạng thái seat, chọn nhân vật 2 bước), đợt 2 (mục 4: seat thu nhỏ khi >6 người), đợt 3 (mục 9: dialog nhật ký/cài đặt/mã phòng), đợt 4 (mục 5: cảnh báo riêng Dynamite/Jail), đợt 5 (mục 4: viên đạn thay tim máu + lá nhân vật inline — **mục 4 coi như xong đủ 3 ý**), đợt 6 (mục 8: băng thông báo phản ứng đầu bàn), và đợt 7 (mục 7: khu giữa bàn — bộ bài rút/chồng bỏ) đã xong, xem changelog "Giao diện UI/UX — đợt 1" tới "đợt 7" trong CHANGELOG.md. **Mục 6/9 (phần còn lại — số ghế/thứ tự lượt, UI thật cho 4 nút Nhật ký/Thư viện/Mã phòng/Cài đặt, nút Đầy đủ/Gọn cho trang bị người khác) ĐÃ XONG (2026-09-08)** — xem "Thiết kế lại UI bàn chơi trong ván" ở cuối mục "Trạng thái hiện tại" này. Coi như **đủ cả 9/9 mục** của `docs/ui-ux/GIAO-DIEN-UI-UX.txt`. Việc 5.3 (house rules) có 4/6 luật nháp ban đầu (kèm UI chọn thật). **Việc 5.4 (mở rộng Dodge City) đang làm — đã ĐỦ 40/40 lá bài trong `core/`** (mục A + B + E, xem changelog "Mở rộng Dodge City — đợt 1"/"đợt 2" trong CHANGELOG.md), luật riêng biến thể 3 người (mục D — thưởng 3 lá khi tự tay hạ bất kỳ ai, event `HUNT_KILL_BOUNTY_DRAWN` mới), và UI hiển thị chất bài (mục F — rà lại code phát hiện ĐÃ có sẵn từ đợt 1, badge "8♥"/"K♦" trên mọi lá tay/trang bị, ghi chú cũ chỉ bị lỗi thời) đều đã xong. **Mục C (15/15 nhân vật) HOÀN TẤT** — nhóm A (7 người), nhóm B (4 người), nhóm C (3 người) VÀ Vera Custer (nhân vật cuối cùng, cơ chế uỷ quyền toàn hệ thống hook) đều đã xong (core + test, UI CHƯA có nút bấm thật). Chỉ còn UI thật cho toàn bộ 40 lá/trang bị trì hoãn/mục D/15 nhân vật — xem "Ghi chú cho 5.4" trong `docs/roadmap/LO-TRINH.md` để biết chi tiết đầy đủ. **Việc 5.5 (mở rộng High Noon) đang làm — 12/13 lá sự kiện đã xong trong `core/`** (`core/events.ts` + hạ tầng chồng sự kiện ở `applyTurnStartChecks()`/`setup.ts`, xem changelog "Mở rộng High Noon — đợt 1"/"đợt 2" trong CHANGELOG.md): Hangover/The Doctor/The Daltons/High Noon (đợt 1) + Gold Rush/Shootout/The Reverend/The Sermon/Thirst/Train Arrival/Blessing/Curse (đợt 2) đều xong core+test. Chỉ còn **Ghost Town** (khó nhất bộ, CỐ TÌNH để dành làm CHUNG với lá Dead Man bên A Fistful of Cards — 2 lá cùng cần cơ chế "người chết vẫn vào vòng lượt", làm riêng rẽ sẽ phải sửa lại). **Việc 5.6 (mở rộng A Fistful of Cards) đang làm — 11/13 lá đã xong trong `core/`** (xem changelog "Mở rộng A Fistful of Cards — đợt 1"/"đợt 2" trong CHANGELOG.md, đọc `docs/bang-rules/Luat_Bang_Mo_Rong_FistfulOfCards.txt` trước khi động vào bộ này): Lasso/Sniper/Ambush/The Judge/Blood Brothers/Vendetta/Hard Liquor/Ricochet/Ranch/**A Fistful of Cards (lá cuối)**/**Russian Roulette** đều xong core+test (mỗi lá 1 file `test/fistful-*.test.ts` riêng). Ambush có 1 mâu thuẫn *dev-cũ-vs-FAQ đã hỏi lại chủ dự án và CHỐT theo FAQ Q17 (xem file luật). Abandoned Mine giữ nguyên bị loại khỏi bộ bốc (chủ dự án đã chốt từ trước). **Peyote thiết kế đã CHỐT ĐỦ (hết giờ tự đoán "đỏ"; va chạm 8 nhân vật override bước rút bài giải quyết bằng 1 cổng `NEED_GUESS_CARD_COLOR` chung, xem ghi chú *dev 2026-08-08 trong file luật) nhưng TẠM DỪNG chưa viết code theo yêu cầu chủ dự án** — làm tiếp khi quay lại, không cần hỏi lại thiết kế. **Còn lại 2 lá khó nhất chưa động tới**: Dead Man (cố tình làm CHUNG với Ghost Town bên High Noon — cùng cần cơ chế "người chết vẫn vào vòng lượt") và Law of the West (khó nhất, cần kiến trúc "nước đi bắt buộc" hoàn toàn mới).

**UI cho High Noon + A Fistful of Cards (2026-08-08) — ĐÃ CÓ nút bấm thật, TẠM GỘP thành 1 nút** (thay vì 2 checkbox riêng): vì mỗi bộ RIÊNG LẺ vẫn còn thiếu vài lá (Ghost Town/Dead Man/Law of the West/Peyote), tách 2 nút sẽ để lộ bộ bài quá mỏng — màn hình thiết lập ván (hotseat lẫn qua mạng, dùng chung `renderExpansionCheckboxes()`) giờ có 1 checkbox "Lá sự kiện — High Noon + A Fistful of Cards (mở rộng, gộp chung)" bật CẢ 2 id `"high_noon"`/`"a_fistful_of_cards"` cùng lúc (`toggleExpansionId()` ở `main.ts`). Luật "random 1 trong 2 lá cuối khi cả 2 bộ cùng bật" ĐÃ CÓ SẴN từ trước trong `setup.ts`, không đổi gì. 4 lá chưa cài (Ghost Town/Dead Man/Law of the West/Peyote) bị loại tạm khỏi `EXPANSION_EVENT_IDS` (`core/events.ts`) — cùng cơ chế đã dùng cho Abandoned Mine — nên KHÔNG thể bị lật trúng trong bộ bốc thật (`EventDefinition` vẫn khai báo đủ, chỉ gỡ khỏi khi nào cài xong lá đó). Đã tự kiểm bằng trình duyệt thật (`npm run dev`, hotseat 4 người): tick nút, chơi vài lượt, log ghi đúng "Lá sự kiện mới: The Reverend" ở đúng lượt thứ 2 của chủ trò, không lỗi console.

**4 việc UI/UX bổ sung (2026-08-08)** — xem changelog "4 việc UI/UX bổ sung" trong CHANGELOG.md để biết chi tiết đầy đủ: (1) khu giữa bàn giờ hiện đúng lá sự kiện đang chạy + lá kế tiếp (giải quyết hạn chế đã ghi ở mục ngay trên); (2) bỏ hẳn ép xoay ngang trên điện thoại — chiều dọc chơi được; (3) Thư viện bài bổ sung 2 nhóm từng bị bỏ sót hoàn toàn (13 lá vàng Dodge City + 23 lá sự kiện High Noon/A Fistful of Cards đang hoạt động thật); (4) thêm ô tìm kiếm đầu thư viện (cả màn hình đầy đủ lẫn dialog trong ván) + đổi layout grid sang list cho dễ đọc. Đã tự kiểm bằng trình duyệt thật, không lỗi console.

**4 nhân vật *ex đợt "dễ trước" (2026-08-10)** — xem `docs/bang-rules/House_Rule.txt` mục I để biết chi tiết luật/thiết kế: **The Thief** (3 máu — draw! cộng thêm sau pha rút bình thường, ra đỏ thì cướp ngẫu nhiên 1 lá của 1 người khác), **Jonny Bettor** (3 máu — USE_ABILITY bỏ 2 lá rồi draw!, đỏ rút 3/đen rút 1, không giới hạn số lần), **The DareDevil** (4 máu — USE_ABILITY mất 1 máu bắn Bang! bỏ qua khoảng cách, 1 lần/lượt, không hoàn máu dù bị đỡ), **Victor Boozer** (4 máu — hoàn toàn thụ động, ăn theo +1 máu mỗi khi NGƯỜI KHÁC uống Bia thành công, hook mới `onOtherPlayerHealedByBeer`) đều đã **CODE XONG** (core + UI hotseat/qua mạng, trừ Victor Boozer không cần UI vì thụ động) + test riêng từng nhân vật (`test/custom-characters/the-thief.test.ts`/`the-gambler.test.ts`/`the-fair-killer.test.ts`/`the-drunker.test.ts`, tổng 41 test). Cả 4 đều thuộc bộ mở rộng `"custom_characters"` (checkbox "Nhân vật *ex" lúc thiết lập ván) — cộng 3 nhân vật cũ (Elena Noir/Marcel Marcelo/Mary Rose) thành **7/13 nhân vật *ex đã xong**.

**Nomad Norman *ex (2026-08-10) — CODE XONG** (core + UI hotseat/qua mạng + test — xem đặc tả đã CHỐT ở `docs/bang-rules/House_Rule.txt` mục I, đoạn "Nomad Norman 3 máu", KHÔNG phải bản mô tả cũ đã bị bỏ). 3 máu, 2 chất đỏ (Cơ/Rô). Đầu mỗi lượt của chính mình, draw! 1 lá **NGẦM** (chỉ mình biết): đỏ thì có 1 "lá chắn" tới đầu lượt kế tiếp của chính mình; khi sắp mất máu vì bất cứ lý do gì có thể tự chọn dùng lá chắn để chặn **TRỌN** cả cụm sát thương đó (kể cả Thuốc nổ 3 máu). Test: `test/custom-characters/the-drifter.test.ts` (21 test). **2 phát hiện kiến trúc quan trọng lúc code** (không có trong đặc tả gốc, tự rà code phát hiện): (1) "applyDamage() là điểm áp sát thương duy nhất" — SAI, có 6 điểm thật (4 qua `applyDamage()`: Indians!/Bang!-Gatling/Duel/High Noon turn-start; 2 điểm tự trừ máu trực tiếp KHÔNG qua `applyDamage()`: Dynamite/Russian Roulette) — cả 6 đều đã cắm `maybeAskDrifterShield()`; (2) draw! bí mật KHÔNG được tái dùng nguyên xi `NEED_DRAW_CHECK`/`resolveDrawCheck()` thường vì sẽ rò rỉ qua `discardPile` (công khai qua `viewFor()`) lẫn qua `events` (server gửi NGUYÊN VẸN cho MỌI socket, KHÔNG lọc theo viewer, khác `view` — xem `room.ts`'s `sendStateTo()`) — phải đi 1 nhánh riêng trong `resolveDrawCheck()`, giữ lá ở field nội bộ mới `GameState.drifterHiddenCard` (không lộ qua `PlayerView`), không emit event nào. `GameState.drifterShield` (Record theo playerId, để tương thích Vera Custer mượn) cũng là field **BÍ MẬT ĐẦU TIÊN** trong dự án cần `viewFor()` lọc riêng theo viewerId (khác `elenaNoirArmed` công khai) — `PlayerView.drifterShield` chỉ chứa ĐÚNG 1 key của chính viewer nếu có, người khác không thấy key đó dưới bất kỳ dạng nào. `NEED_USE_DRIFTER_SHIELD` (pending mới) đẩy **VÔ ĐIỀU KIỆN** mỗi khi mục tiêu có khả năng này — kể cả đang KHÔNG có khiên thật — vì chỉ đẩy lúc có khiên thật sẽ khiến chính sự XUẤT HIỆN của pending lộ bí mật (đúng mẫu `NEED_MISSED` vẫn hỏi dù không có gì để đỡ). Đã tự kiểm bằng trình duyệt thật (hotseat 8 người, bật "Nhân vật *ex"): tra "Nomad Norman" trong Thư viện bài ra đúng mô tả, màn hình chọn nhân vật hiển thị đúng, không lỗi console (chưa random trúng đúng Nomad Norman trong ván thật để tự kiểm luồng dùng khiên qua UI — rủi ro thấp, logic đã có 21 test bao phủ đầy đủ các nhánh).

**Envoy Evy *ex (2026-08-10) — CODE XONG** (core + UI hotseat/qua mạng + test — xem đặc tả đã CHỐT ở `docs/bang-rules/House_Rule.txt` mục I, đoạn "Envoy Evy 4 máu"). 4 máu. Mỗi khi bị nhắm bởi 1 đòn "kiểu Bang!" (Bang!, Gatling, Punch, Springfield, Derringer, Knife, Pepperbox, Buffalo Rifle, Howitzer, đòn của Doc Holyday...) và không đỡ được, được chọn đưa **2 lá NGẪU NHIÊN** trên tay cho **NGƯỜI VỪA ĐÁNH mình** để vô hiệu đòn đó — không giới hạn số lần, miễn còn đủ 2 lá. Test: `test/custom-characters/the-dealer.test.ts` (12 test). **Tự rà code trước khi cài và xác nhận CHỈ CÓ ĐÚNG 1 điểm cắm** (khác Nomad Norman — 6 điểm): mọi lá "kiểu Bang!" đều đi qua CHUNG 1 con đường `pushMissedReactionUnconditional()` → `NEED_MISSED` → `respondToMissed()` — tái dùng gần như nguyên vẹn hạ tầng vừa xây cho Nomad Norman (cùng vị trí chèn trong `respondToMissed()`, ngay trước `applyDamage()`, không xung đột vì 1 người không thể vừa là Nomad Norman vừa là Envoy Evy). **Khác Nomad Norman — KHÔNG cần "hỏi vô điều kiện"**: "còn đủ 2 lá" không phải bí mật (`handCount` vốn đã công khai), nên `NEED_USE_DEALER_TRADE` chỉ đẩy khi THẬT SỰ đủ điều kiện (còn ≥2 lá VÀ có người đánh còn sống để đưa bài — đã tự phát hiện và chặn ca biên: lá sự kiện "A Fistful of Cards" bắn với `source.from: null`, không có ai để đưa bài, phải bỏ qua). Event riêng `DEALER_TRADE_USED` (KHÔNG tái dùng `CARD_STOLEN` có sẵn — đã kiểm `describeEvent()` phrasing "X cướp Y của Z" sai ngữ nghĩa cho hành động tự nguyện). Đã tự kiểm bằng trình duyệt thật: tra "Envoy Evy" trong Thư viện bài ra đúng mô tả, không lỗi console.

**Aura The Soul-Weaver *ex (2026-08-20) — CODE XONG** (core + UI hotseat/qua mạng + test — xem changelog "Aura The Soul-Weaver *ex" trong CHANGELOG.md). 3 máu (đã hỏi lại chủ dự án để chốt số liệu — đặc tả chính ghi 3 máu, phần đánh giá sức mạnh phía dưới file luật ghi nhầm 4 máu do bản nháp cũ). Bất kỳ lúc nào có người sắp chết (kể cả chính Aura The Soul-Weaver), hỏi SAU CÙNG có muốn trả 2 máu tối đa VĨNH VIỄN để hồi sinh 1 máu hay không — ĐÚNG 1 LẦN CẢ VÁN. Lần đầu dự án sửa `maxHp` giữa ván. Điểm khó nhất: tái dùng kiểu `resume` của Nomad Norman (đổi tên `DrifterShieldResume` → `DamageResume`, dùng chung) để hoãn đúng phần epilogue phụ thuộc `target.alive` (`continueAfterMissedResolved()`/`continueTurnStartAfterHighNoonDamage()`/`applyJailCheck()`) tới sau khi Aura The Soul-Weaver trả lời, còn phần không phụ thuộc alive (Mary Rose reflect/Molly Stark) vẫn chạy ngay, không cần hoãn. Test: `test/custom-characters/the-sentinel.test.ts` (10 test). Đã tự kiểm bằng trình duyệt thật (hotseat 6 người): random trúng đúng nhân vật, hạ máu về 0, hồi sinh chính mình qua UI, máu tối đa/hiện tại/nhật ký đều đúng, không lỗi console.

**The Nobody *ex — NHÓM A HOÀN TẤT ĐỦ 5/5 (2026-08-20) — CODE XONG** (core + test — xem changelog "The Nobody *ex — NHÓM A" và "The Nobody *ex — Cửa hàng tổng hợp" trong CHANGELOG.md). File luật tự nhận đây là "nhân vật *ex tốn công nhất từ trước tới nay". 3 máu, bị nhắm tới bởi bất kỳ lá nào thì bắt buộc draw! 1 lá, ra Bích thì lá đó vô hiệu hoàn toàn. Đủ 5 điểm cắm nhóm A: Bang!/Gatling (nhóm "kiểu Bang!"), Indians!, Đấu tay đôi, Cat Balou/Can Can/Brawl (4 điểm dùng chung 1 mẫu — tái dùng cách Barrel đẩy `NEED_DRAW_CHECK` lên trên pending gốc, hàm `maybePushNobodyDrawCheck()`), và **Cửa hàng tổng hợp** (lá khó nhất, phải tách `playGeneralStore()` thành 2 giai đoạn vì số lá lật ra phụ thuộc The Nobody có bị bỏ qua hay không — draw! phải xong TRƯỚC khi biết lật mấy lá, side-channel mới `GameState.pendingGeneralStore` cùng khuôn `duelBangDrawPending`). Draw! của The Nobody luôn đọc chất THẬT (không qua `getEffectiveSuit()`) để né bẫy Blessing/Curse đã chốt trong file luật. Test: `test/custom-characters/the-nobody.test.ts` (12 test). Đã tự kiểm bằng trình duyệt thật 2 lần (hotseat 8 người) — Cat Balou nhắm vào anh ta (pending xếp đúng thứ tự) và tự đánh Cửa hàng tổng hợp (nhật ký ghi đúng "lật 7 lá" khi 8 người sống trừ 1 bị bỏ qua, người chọn đầu tiên nhường đúng sang người kế) — không lỗi console.

**The Nobody *ex — NHÓM B, HOÀN TẤT TOÀN BỘ NHÂN VẬT (2026-08-20) — CODE XONG** (core + test — xem changelog "The Nobody *ex — NHÓM B" trong CHANGELOG.md). 5 lá cuối cùng (Panic!/Rag Time/Conestoga, Jail, Saloon, Tequila, Marcel "cùng vào tù") — khác hẳn nhóm A: các lá này áp dụng NGAY LẬP TỨC, không có pending sẵn để huỷ, phải viết lại thành "hoãn rồi mới áp dụng". State mới `GameState.pendingNobodyCheck` (discriminated union 4 kind, gộp chung thay vì 5 field riêng — Tequila không cần field này). Panic! tách hàm để giữ RNG tất định (xác định lá sẽ bị cướp NGAY, chỉ hoãn phần chuyển lá); Jail hoãn phần gắn lá (khớp thì lá phải có chỗ đi — bỏ vào chồng bỏ); Saloon hồi máu người khác NGAY LẬP TỨC (khác Cửa hàng tổng hợp — không phụ thuộc lẫn nhau), chỉ hoãn phần của The Nobody; Marcel companion hoãn việc GHI NHẬN (khớp thì Marcel coi như không có ai cùng vào tù). Bẫy Blessing/Curse gộp chung qua `source.card.startsWith("the_nobody")`. Test: `test/custom-characters/the-nobody.test.ts` (22 test, +10 so với đợt trước). Đã tự kiểm bằng trình duyệt thật (hotseat 8 người): đánh Nhà tù và Saloon vào The Nobody (Bình) — băng thông báo/log đúng, không khớp thì áp dụng bình thường (Jail gắn được, Saloon hồi máu) — không lỗi console. **THE NOBODY *EX HOÀN TẤT TOÀN BỘ 10/10 ĐIỂM CẮM** (5 nhóm A + 5 nhóm B), không còn phần nào bị bỏ dở.

Còn lại Jeff The Clown trong `docs/bang-rules/House_Rule.txt` mục I (nên làm SAU CÙNG theo đúng ghi chú trong file luật, và giờ là *ex duy nhất còn lại) — CHƯA làm, để dành đợt sau. Paul Pauper đã CODE XONG, xem đoạn dưới.

**Sửa UI nhấn giữ lá bài trên di động (2026-08-20) — ĐÃ SỬA, đã lên cả 2 bản (commit `5cc6505`)**: chủ dự án báo nhấn giữ 1 lá bài (`attachDescriptionReveal()` ở `ui.ts`) rất khó đọc mô tả — 2 vòng sửa liên tiếp trong cùng ngày. **Vòng 1** — nguyên nhân thật: ngón tay chạm thẳng vào thẻ `<img>` bên trong lá, trình duyệt di động tự bật menu "lưu ảnh"/callout riêng, chen ngang. Thêm 1 lớp phủ trong suốt phủ TOÀN BỘ lá (`.hold-description-overlay`, luôn hiện diện — không `display: none` khi nghỉ — để chắc chắn hứng được `touchstart`) thay cho popup nổi cũ gắn vào `document.body` (`.card-description-popup`, đã BỎ HẲN). Giữ đủ 500ms thì chính lớp phủ đổi nền tối + hiện chữ NGAY TRÊN vị trí lá (đè lên ảnh) thay vì bung popup ở chỗ khác — mô tả dài tự tràn xuống dưới khung lá gốc (`.card-box--holding` tạm bỏ `overflow: hidden` + đẩy `z-index` lên trên lá bên cạnh). **Test thật lần 1 THẤT BẠI** — chủ dự án báo "vẫn hiện menu lưu ảnh y như cũ": bug ở chỗ `-webkit-touch-callout`/`user-select` lúc đó gán TRÊN CHÍNH LỚP PHỦ (phần tử anh em của `<img>`, không phải cha/ông) — CSS kế thừa chỉ lan xuống con cháu, gán ở phần tử anh em vô tác dụng với `<img>`. **Vòng 2 (sửa đúng)**: chuyển các thuộc tính đó (`-webkit-touch-callout`, `user-select`, `-webkit-user-drag`) + `pointer-events: none` sang gán TRỰC TIẾP trên chính `<img>` (`.card-box__image` và `.table-center__event-image`) — đảm bảo mọi chạm luôn rơi vào lớp phủ (nằm trên, `z-index` cao hơn) chứ không bao giờ tới `<img>` nữa, dù layer stacking có trục trặc gì. Test thật lần 2 mới THÀNH CÔNG. **Tinh chỉnh thêm cùng ngày** (chủ dự án báo tiếp: ngón tay đang giữ che mất chữ vừa hiện ra) — đổi `touchmove`: SAU KHI lớp phủ đã hiện (`triggered = true`) thì bỏ qua hoàn toàn, cho phép kéo tay ra chỗ khác trên màn hình mà chữ không biến mất (sự kiện touch luôn bám đúng phần tử `el` ban đầu dù ngón tay di chuyển tới đâu, không cần re-target); ngưỡng `MOVE_CANCEL_THRESHOLD_PX` cũ (phân biệt rung tay/trượt tay thật) chỉ còn áp dụng TRƯỚC khi lớp phủ hiện. Chỉ nhấc tay (`touchend`)/`touchcancel` mới ẩn. `tsc --noEmit` + `vitest run` (763 test) đều pass sau cả 2 vòng sửa (thay đổi CSS/DOM thuần, không đụng `core/`). Đã tự kiểm bằng điện thoại thật qua bản beta ở CẢ 3 lần (test lỗi lần 1, xác nhận sửa đúng lần 2, xác nhận tinh chỉnh kéo tay) trước khi đẩy lên bản chính — bundle hash đối chiếu khớp nhau trên cả 2 URL sau deploy cuối.

**3 việc sửa/bổ sung (2026-08-22) — ĐÃ SỬA, đã lên cả 2 bản**: (1) **Bug thật tiếp theo của lỗi nhấn giữ lá bài trên di động** — nguyên nhân mới phát hiện (khác 2 vòng sửa CSS/DOM ngày 2026-08-20 ở trên): `countdownTickId` (đồng hồ đếm ngược, gần như luôn chạy suốt ván qua mạng) gọi `render()` mỗi giây, mà `render()` dựng lại TOÀN BỘ DOM kể cả chính lá đang giữ — lá cũ (mang lớp phủ mô tả) bị xoá, lá mới dựng lên không có gì đang hiện dù ngón tay chưa nhả. Đã chặn hẳn `render()` không vẽ lại khi `document.querySelector(".card-box--holding")` còn tồn tại — đợi lượt `render()` kế tiếp (sau `touchend`/`touchcancel`/tự ẩn) mới vẽ lại bù mọi thay đổi bị hoãn. (2) **Bug Vera Custer** — phần client (đáp lại Missed!: `cardMatchesRespondable()`/`hasEnoughMissedToRespond()` ở `ui.ts`) vẫn xét theo `characterId` THẬT của Vera thay vì khả năng đang mượn, sai lệch với core (core đã dùng `getEffectiveCharacterId()` từ đợt Vera Custer trước) — thêm `veraCusterBorrowedCharacterId` vào `PlayerView` (`view.ts`, công khai, không bí mật — giống `elenaNoirArmed`) + hàm `effectiveCharacterId()` mới ở `ui.ts` mirror đúng logic core. (3) **Nút bấm thật cho 5 lá A Fistful of Cards còn thiếu UI** (core đã xong từ đợt 5.6, giờ mới nối UI): Sniper, Ricochet, Hard Liquor, Blood Brothers, Ranch — 4 bước `Selection` mới (`picking-sniper-confirm`/`picking-ricochet-confirm`/`picking-ricochet-equipment`/`picking-ranch-exchange`) + handler tương ứng cho cả hotseat lẫn qua mạng. `tsc --noEmit` + `vitest run` (763 test) đều pass. Đã tự kiểm bằng trình duyệt thật (hotseat 4 người, bật Dodge City + High Noon/A Fistful of Cards cùng lúc): chơi vài lượt rút/đánh/bỏ bài, không lỗi console — **chưa** random trúng đúng Vera Custer hay 5 lá sự kiện mới trong lúc test (rủi ro thấp, đã có test bao phủ đầy đủ các nhánh, đúng nếp cũ của dự án).

**7 nhân vật *ex đổi tên + 2 việc chỉnh luật (2026-08-23) — ĐÃ SỬA**: chủ dự án ghi lại 1 danh sách việc cần làm trong `docs/roadmap/LO-TRINH.md`, làm xong ngay trong ngày (chi tiết đầy đủ xem mục "Việc đã làm xong (ghi lại 2026-08-23)" trong `docs/roadmap/LO-TRINH.md`). Đổi tên hiển thị (giữ nguyên id nội bộ/tên hàm/tên field): The Fair Killer → The DareDevil, The Gambler → Jonny Bettor, The Drunker → Victor Boozer, The Drifter → Nomad Norman, The Dealer → Envoy Evy, The Sentinel → Aura The Soul-Weaver, và Beggar → Paul Pauper (nhân vật này CHƯA CODE, chỉ đổi tên trong đặc tả). Rà bằng 1 script Node.js một lần (đọc/ghi UTF-8 tường minh, khớp theo ranh giới từ để không đụng identifier) trên toàn bộ file chữ (`docs/bang-rules/House_Rule.txt`/`CLAUDE.md`/`CHANGELOG.md`/mọi file `src/`+`test/`), đã quét lại không phát hiện mojibake. 2 chỉnh luật core: **Marcel Marcelo** giờ chỉ rút ĐÚNG 1 lá lúc kiểm tra thoát tù (trước đây tối đa 2 lá — bỏ hẳn nhánh rút lá thứ 2 + event `MARCEL_JAIL_SECOND_DRAW`, mọi cơ chế khác giữ nguyên); **Jonny Bettor** giới hạn còn 2 lần dùng kỹ năng/lượt (trước đây không giới hạn — field mới `GameState.gamblerUsesThisTurn`, mirror đúng khuôn José Delgado, cả nút UI hotseat/qua mạng đều đã cập nhật điều kiện ẩn/hiện). `tsc --noEmit` + `vitest run` (764 test) đều pass. Đã tự kiểm bằng trình duyệt thật (hotseat, bật "Nhân vật *ex", tra Thư viện bài) ngay trong đợt Paul Pauper ngay dưới đây — xác nhận cả 7 tên hiển thị mới VÀ đúng "tối đa 2 lần/lượt" của Jonny Bettor đều hiện đúng trên UI thật, không lỗi console.

**Paul Pauper *ex — CODE XONG (2026-08-23)** — nhân vật *ex khó nhất còn lại ngoài Jeff The Clown (xem `docs/bang-rules/House_Rule.txt` mục I, đoạn "Paul Pauper 4 máu"). Mỗi khi 1 người chơi khác đánh lá thứ 4 trở đi trong lượt của họ, Paul Pauper BẮT BUỘC draw! 1 lần MỖI lá: ra đỏ (Cơ/Rô) thì lá đó bị CHẶN HOÀN TOÀN — không hiệu ứng gì xảy ra, lá đi thẳng vào tay Paul Pauper thay vì chồng bỏ/sân (áp dụng cho MỌI loại lá, kể cả trang bị). Điểm cắm ĐẦU handlePlayCard() (TRƯỚC mọi play*()) — lá VẪN CÒN NGUYÊN trong tay người đánh trong lúc chờ draw! giải quyết (không splice/discard trước), state mới `GameState.cardsPlayedThisTurn`/`pendingPaulPauperPlay`, tham số nội bộ `skipPaulPauperCheck` để `handlePlayCard()` gọi lại chính nó sau khi draw! ra đen mà không hỏi lại vô hạn. Chốt lại với chủ dự án (qua AskUserQuestion): lá bị chặn KHÔNG tính vào `cardsPlayedThisTurn` — đúng nghĩa đen "coi như lá chưa từng được đánh". UI KHÔNG cần thêm gì — dùng chung nút "Lật bài" có sẵn cho mọi `NEED_DRAW_CHECK` (giống Barrel/Jail/The Nobody/Jonny Bettor). Test: `test/custom-characters/paul-pauper.test.ts` (11 test). `tsc --noEmit` + `vitest run` (775 test) đều pass. Đã tự kiểm bằng trình duyệt thật (hotseat, bật "Nhân vật *ex", tra "Paul Pauper" trong Thư viện bài) — mô tả đúng, không lỗi console; **nhân tiện phát hiện và sửa luôn 1 bug sót lại** từ đúng đợt sửa Marcel Marcelo phía trên: mô tả Marcel trong Thư viện bài/màn chọn nhân vật (ui.ts) vẫn ghi "rút tối đa 2 lá" — đã cập nhật đúng "rút ĐÚNG 1 lá". Chưa random trúng đúng Paul Pauper trong ván thật lúc tự kiểm (rủi ro thấp, 11 test bao phủ đủ các nhánh, đúng nếp cũ của dự án).

**Thiết kế lại UI bàn chơi trong ván (2026-09-08) — ĐÃ CODE XONG** (theo yêu cầu chủ dự án, mockup preview trước rồi mới code thật, xem `docs/ui-ux/GIAO-DIEN-UI-UX.txt` mục 6/9 còn dang dở từ các đợt trước — đợt này coi như làm xong nốt): (1) **Số ghế/thứ tự lượt** — mọi người chơi giờ có 1 huy hiệu số cố định (①②③...) cạnh ảnh đại diện, số 1 LUÔN là Cảnh sát trưởng (biến thể duel/hunt thì số 1 là người đi đầu) — hàm `computeSeatNumbers()` mới trong `ui.ts`, tính lại từ `firstPlayerIndex` giống hệt `core/setup.ts`, an toàn dùng cho cả hotseat (đọc `PlayerState[]`) lẫn qua mạng (đọc `PlayerView.players` đã lọc — vai trò Cảnh sát trưởng vốn luôn công khai nên không lộ gì thêm). (2) **Chip "Bạn"** — thanh toolbar qua mạng giờ có 1 chip dính đầu (sticky) hiện số ghế + máu của chính người xem, hotseat không có (không có khái niệm "bản thân" khi chung 1 máy). (3) **UI thật cho 4 nút Nhật ký/Thư viện/Mã phòng/Cài đặt** — dựng lại nội dung dialog `renderDialog()` (bỏ tham số `closeButtonAtTop`, nút đóng giờ luôn `position: fixed` nên bất kể dialog dài ngắn cũng không còn bug nhảy cuộn cũ); Cài đặt viết lại bằng 2 helper mới `renderSegmentedControl()`/`renderSwitchRow()`. (4) **Nút "Đầy đủ ↔ Gọn"** — bật "Gọn" thì trang bị của NGƯỜI KHÁC trên sân chỉ hiện tên chữ (không hiện ảnh lá), bài của CHÍNH MÌNH không bao giờ bị ảnh hưởng; hotseat không có ngoại lệ (không ai là "chính mình" khi chung máy), qua mạng thì loại trừ đúng `player.id === view.viewerId`. Lưu `localStorage` riêng (`bang_equipment_compact`), không reset khi bắt đầu ván mới (là sở thích cá nhân, khác `expandedSeatIds`). **1 bug tìm thấy lúc tự kiểm và đã sửa ngay**: `renderSegmentedControl()`/`renderSwitchRow()` (nút bấm thường, không phải radio gốc) lúc đầu chỉ gọi callback mà không tự cập nhật `aria-pressed` — bấm "Tối"/"Lớn" thì hiệu ứng thật đổi đúng nhưng nút không đổi màu ngay, phải rời dialog mở lại mới thấy đúng; đã sửa để 2 hàm tự set `aria-pressed` ngay lúc bấm, trước khi gọi callback. `tsc --noEmit` + `vitest run` (775 test) đều pass (đợt này chỉ sửa `ui.ts`/`main.ts`/`style.css`, không đụng `core/`). **Đã tự kiểm đầy đủ bằng trình duyệt thật** — ưu tiên chế độ qua mạng theo yêu cầu chủ dự án: mở 2 tab `wrangler dev` (An + Bình), xác nhận huy hiệu số ghế đúng cho cả 2 người, chip "Bạn" hiện đúng máu/số ghế, dialog Mã phòng (nút "Sao chép mã" hoạt động thật) và cả 4 dialog còn lại đều đúng thiết kế; xác nhận đúng cơ chế loại trừ trang bị bản thân theo CẢ 2 chiều (An bật "Gọn" thấy trang bị Nhà tù của Bình co lại thành chữ; Bình tự bật "Gọn" trên máy mình thì trang bị của chính Bình vẫn hiện ảnh đầy đủ). Cũng tự kiểm hotseat 4 người (huy hiệu ①②③④ đúng thứ tự, toggle Gọn/Đầy đủ áp dụng đều không phân biệt ai) và mô phỏng màn hình di động dọc (kỹ thuật iframe thu nhỏ, vì sandbox này không resize được cửa sổ thật) — toolbar co lại còn icon (đủ kích thước chạm ≥2.75rem), dialog vừa khung, không tràn ngang. Không lỗi console ở bất kỳ chế độ nào.

**2 việc sửa tiếp theo đợt UI bàn chơi (2026-09-08, cùng ngày với đợt phía trên) — ĐÃ XONG, đã commit (`25345e8`)**: chủ dự án phản hồi "vẫn chưa đẹp bằng bản mockup, thanh trượt bị lạc quẻ so với theme" + yêu cầu bổ sung "trên di động, người chơi khác vẫn phải xếp thành 1 hàng ngang, trượt qua lại để xem/tương tác" (yêu cầu MỚI, ĐỔI lại quyết định cũ ở mục 10 `docs/ui-ux/GIAO-DIEN-UI-UX.txt` — file đó ghi "điện thoại dọc: chuyển sang DANH SÁCH DỌC", giờ chủ dự án chốt lại thành hàng ngang thay vì danh sách dọc, chỉ sửa CSS/style.css nên không cần hỏi lại theo "Cách làm việc"). (1) **Thanh cuộn (scrollbar)** — `.roster` (hàng người chơi màn "Chọn nhân vật") vốn cuộn ngang (`overflow-x:auto`) nhưng KHÔNG có CSS riêng, nên Windows/Chrome tự vẽ thanh cuộn "cổ điển" (xám sáng + 2 nút mũi tên 2 đầu) — đúng thứ chủ dự án chê "lạc quẻ". **2 vòng sửa**: vòng 1 thử tô lại màu qua `scrollbar-color`/`::-webkit-scrollbar-thumb` + ẩn nút qua `::-webkit-scrollbar-button{display:none}` — `getComputedStyle` xác nhận CSS áp dụng đúng (`buttonDisplay:"none"`) nhưng 2 nút mũi tên VẪN hiện trên thực tế (giới hạn vẽ của Windows Chrome bản "cổ điển", không tuân theo CSS ở bước vẽ cuối — đã xác minh qua `getComputedStyle` gọi trực tiếp vào `contentDocument` của 1 trang test cùng-nguồn gắn tạm `public/_mobile_test.html`, đã xoá sau khi xong). Vòng 2 (sửa đúng, ĐANG ÁP DỤNG): bỏ hẳn ý định tô màu, **ẨN HẲN thanh cuộn** ở đúng 2 khung "carousel vuốt ngang" (`.roster`, `.players` — dải ghế người chơi trong ván) qua `scrollbar-width:none` + `::-webkit-scrollbar{display:none}`, giống cách Instagram/Netflix ẩn thanh cuộn ở dải thẻ vuốt ngang; cuộn/vuốt vẫn hoạt động, chỉ không có thanh nhìn thấy được. Các khung cuộn KHÁC (dialog, `.log-list`) vẫn giữ thanh cuộn mảnh tô theo theme (`:root { scrollbar-color/scrollbar-width }` + `::-webkit-scrollbar-thumb/track` — phần CSS này KHÔNG bỏ, chỉ không đủ để tự nó xoá 2 nút mũi tên trên Windows, nhưng không sai/không hại gì ở nơi khác). (2) **Người chơi khác xếp hàng ngang trên di động** — trước đợt này CHỈ qua mạng có (`.opponent-row`, đã có sẵn `flex-wrap:nowrap`+`overflow-x:auto` ở MỌI cỡ màn hình từ đợt cũ); HOTSEAT (`.players` dùng chung cho MỌI người chơi, không tách "hàng riêng của mình" vì hotseat không có khái niệm "bản thân") vẫn `flex-wrap:wrap` nên trên di động bị xếp DỌC từng người 1 hàng (đúng thứ chủ dự án chê). Sửa trong `@media (max-width:480px)`: `.players{flex-wrap:nowrap;overflow-x:auto;scroll-snap-type:x mandatory}` (áp dụng luôn cho `.opponent-row` vì cùng class `.players`, không đổi gì thêm ở đó) + `.player{flex:0 0 92%;scroll-snap-align:start}` (92% thay vì 100% để hé lộ 1 mẩu thẻ kế tiếp, gợi ý còn người để vuốt tiếp — không phải viết chọn 100% che kín). Đã tự kiểm bằng trình duyệt thật (kỹ thuật: gắn tạm `<iframe>` cùng-nguồn `public/_mobile_test.html`, cỡ 390×760, thao tác qua `contentDocument`/`contentWindow` để bấm nút và đo `scrollWidth`/`clientWidth`/gán `scrollLeft` — đáng tin hơn hẳn kỹ thuật iframe khác-nguồn dùng trước đây vì soi được DOM/computed style thật bên trong, đã xoá file sau khi xong; công cụ `resize_window` xác nhận LẠI vẫn không hoạt động trong sandbox này — `window.innerWidth` không đổi sau khi gọi, đúng ghi chú cũ): xác nhận `.roster` không còn thanh cuộn nào (trước/sau khi vuốt); xác nhận `.players` hotseat (4 người) cuộn ngang đúng — `scrollWidth` (1358px) > `clientWidth` (356px), gán `scrollLeft=300` di chuyển đúng sang thẻ người chơi kế tiếp ("Bình"), có hé mép thẻ kế tiếp bên phải. `tsc --noEmit` + `vitest run` (775 test) đã chạy PASS sau khi dọn file test tạm. **Đã kiểm lại `.opponent-row` qua mạng trên di động (2026-09-08, phiên sau)**: mở `wrangler dev`, 2 tab thật (An tạo phòng + Bình vào bằng mã), rồi mô phỏng máy Bình ở 390×760 bằng đúng kỹ thuật iframe cùng-nguồn (`dist/_mobile_test.html`, tạo tạm rồi xoá ngay sau khi xong — `resize_window` lại xác nhận KHÔNG đổi `window.innerWidth` thật trong sandbox này, đúng ghi chú cũ). Đọc `getComputedStyle` trực tiếp trong `contentDocument`: `.opponent-row` có `className: "players opponent-row"` (đúng như suy luận — dùng chung selector `.players` nên ăn media query `@media (max-width:480px)` y hệt), `flexWrap: nowrap`, `overflowX: auto`, `scrollbarWidth: none` (không còn thanh cuộn xấu); phần tử `.player` con có `flexBasis: 92%`, `scrollSnapAlign: start` — khớp 100% với CSS đã viết. Phòng test chỉ có 2 người nên không tự thấy được cảnh cuộn ngang thật (chỉ 1 đối thủ, `scrollWidth === clientWidth`), nhưng cơ chế CSS đã xác nhận giống hệt trường hợp hotseat 4 người đã kiểm ở đợt trước. Không lỗi console. Coi như xong hẳn, không còn việc gì dở dang từ đợt UI bàn chơi 2026-09-08.

**BUG tràn khỏi ô người chơi trên di động (phát hiện + sửa 2026-09-08, phiên sau nữa)** — chủ dự án báo lại "khi chơi nhiều người ở chế độ di động màn hình dọc dễ gặp tình huống nội dung tràn khỏi ô của người chơi" và yêu cầu tự test trực tiếp (hotseat 4 người) để tìm lỗi. **Nguyên nhân**: hàng đầu mỗi ô người chơi (`.player__heading-row` ở `ui.ts`, dùng CHUNG cho cả hotseat lẫn qua mạng) xếp ngang huy hiệu số ghế (cỡ cố định) + lá nhân vật (cỡ cố định 4.5rem) + tên người chơi (`<h3>`) — CSS cũ không có `min-width: 0` cho `<h3>`, mà mặc định flexbox KHÔNG BAO GIỜ co 1 ô nhỏ hơn từ dài nhất bên trong nó. Tên người chơi là 1 từ dài không dấu cách (nickname kiểu "SieuNhanGaoVietNam2026", hoặc tên gõ liền không dấu cách — rất phổ biến, ô nhập tên không giới hạn độ dài) sẽ tràn ra ngoài viền `.player` ngay khi ô bị thu hẹp còn ~92% bề rộng màn hình (di động dọc, xem đợt sửa ngay phía trên). **Đã tự kiểm chứng bằng trình duyệt thật trước khi sửa**: hotseat 4 người ở 390×760 (kỹ thuật iframe cùng-nguồn quen thuộc, gắn tạm `public/_mobile_test.html`, đã xoá sau khi xong), gán tên 1 người chơi thành 1 từ dài không dấu cách qua DOM — đo được `player.scrollWidth` (338px) > `clientWidth` (326px), tràn ~12px; zoom ảnh chụp màn hình xác nhận chữ tràn ra ngoài viền ô, không có nền/viền bao quanh phần tràn. **Sửa**: `.player__heading-row h3` thêm `flex: 1 1 auto; min-width: 0; overflow-wrap: anywhere;` (`public/style.css`) — `flex: 1 1 auto` để `<h3>` nhận đúng phần bề rộng CÒN LẠI sau 2 ô cỡ cố định đứng trước (không phải tự tính theo nội dung của chính nó), `min-width: 0` bỏ ngưỡng "không co nhỏ hơn nội dung", `overflow-wrap: anywhere` cho phép ngắt NGAY GIỮA 1 từ dài khi hết chỗ thay vì tràn ra ngoài. Dùng chung 1 class CSS cho cả hotseat (`renderPlayer()`) lẫn qua mạng (`networkRenderPlayer()`) nên sửa 1 chỗ là khớp cả 2 chế độ, không cần sửa gì ở `ui.ts`. **Đã kiểm lại sau khi sửa** (vẫn trong cùng phiên trình duyệt, không cần chơi lại từ đầu — chỉ cần tải lại `style.css`): tên dài không dấu cách giờ tự ngắt xuống dòng 2 ("SieuNhanGaoVietNam2" / "026 ← đang tới lượt"), `scrollWidth === clientWidth` (326 = 326, hết tràn); thử thêm ca cực đoan 65 ký tự liền không dấu cách cũng không tràn (`scrollWidth === clientWidth` = 323). `tsc --noEmit` + `vitest run` (775 test) đều pass (chỉ sửa CSS, không đụng `core/`).

**4 việc sửa UI/UX bổ sung (2026-09-09) — ĐÃ SỬA, TEST theo yêu cầu chủ dự án (đẩy thẳng lên bản chính, nhưng có thể revert dễ dàng — xem commit trước đó `6687822`)**: chủ dự án báo 1 loạt việc trong 1 tin nhắn — màn "Chọn nhân vật" mất cân đối/chưa đẹp, nút đóng dialog + công tắc gạt Cài đặt bị "kéo ra" biến dạng, khung Nhật ký chưa lấp đầy panel, và muốn dời băng thông báo phản ứng lên ngay dưới hàng nút toolbar. **Đã tự kiểm bằng trình duyệt thật (hotseat 4 người, cả desktop lẫn mô phỏng 390×760) TRƯỚC khi sửa để xác định đúng nguyên nhân, không đoán mò**:
1. **Màn "Chọn nhân vật" mất cân đối** — `.duel` (2 lá + chữ "HOẶC", `style.css`) dùng `grid-template-columns: 1fr auto 1fr`: 2 cột lá CHIẾM HẾT bề rộng `.pick-section` (gần 60rem trên màn rộng) nhưng lá nhân vật lại có `width: 9rem` cố định — lá chỉ nằm ở ĐẦU mỗi cột rộng, để lại khoảng trắng chết rất lớn 2 bên, "HOẶC" trôi nổi cô lập giữa màn hình. Đổi sang `grid-template-columns: auto auto auto; justify-content: center` — mỗi cột chỉ rộng bằng đúng nội dung, cả cụm 3 cột co gọn lại rồi canh giữa, giống đúng kiểu "so tài 1 chọi 1". Đã tự kiểm cả desktop (cụm co gọn, canh giữa) lẫn mobile (`@media max-width:30rem` giữ nguyên 1 cột dọc, không đổi gì, vẫn đẹp).
2. **Nút đóng dialog "✕" + công tắc gạt Cài đặt bị kéo méo trên di động** — CÙNG 1 nguyên nhân: luật `@media (max-width:480px) { button { min-height: 2.75rem } }` (đợt cũ, tăng vùng chạm ngón tay) áp dụng cho MỌI `<button>` kể cả 2 nút hình tròn/pill cỡ nhỏ CỐ Ý (`.dialog__close` cao 1.9rem=30.4px, `.switch` cao 1.5rem=24px) — CSS `min-height` luôn là NGƯỠNG SÀN, thắng `height` khai nhỏ hơn nó bất kể thứ tự khai báo, nên cả 2 bị ép cao 44px: nút đóng thành HÌNH TRỨNG, công tắc thành gần TRÒN thay vì pill dẹt. Đã tự đo `getComputedStyle` xác nhận đúng (`.switch` height 44px dù CSS ghi 1.5rem) TRƯỚC khi sửa. Sửa: thêm `.dialog__close, .switch { min-height: 0 }` trong cùng khối media query. Đã tự kiểm lại — cả 2 về đúng hình dạng gốc.
3. **Khung Nhật ký chưa lấp đầy panel trên di động** — `.log-list` có `max-height: 16rem` cố định (hợp lý cho dialog NHỎ tự co theo nội dung ở màn rộng) nhưng dialog trên di động đã đổi sang cao HẾT màn hình (`height: 100%`, đợt cũ) — log chỉ vài dòng bị nhốt trong khung 16rem, để trống khoảng lớn phía dưới dù còn chỗ. Sửa: `@media max-width:480px { .log-list { max-height: none } }` — danh sách tự cao theo đúng nội dung, dài hơn màn hình thì chính `<dialog>` cuộn (y hệt cách dialog "Thư viện bài" vốn đã cuộn đúng, không cần trần riêng). Đã tự kiểm với 9 dòng log thật — trước sửa bị cắt ở khung ~16rem, sau sửa hiện đủ liền mạch (`scrollHeight === clientHeight` = 326px).
4. **Dời băng thông báo phản ứng (+ đồng hồ đếm nếu có) lên ngay dưới hàng nút toolbar** — trước đây gọi `renderPendingPanel()`/`networkRenderPendingPanel()` khá muộn (sau khu giữa bàn/nhãn luật bổ sung/thông báo draw!), giờ dời hẳn lên NGAY SAU `renderGameToolbar()`, trước cả báo lỗi/tóm tắt lượt — áp dụng cho CẢ hotseat (`renderApp()`) lẫn qua mạng (`renderNetworkGame()`, gộp cùng `renderCountdown()` đồng hồ lượt bình thường — 2 hàm loại trừ nhau theo `pending.length`, không bao giờ cùng hiện). Thuần di chuyển lời gọi hàm, không đổi logic bên trong `renderPendingPanel()`. Đã tự kiểm — băng thông báo "⚠ Đang chờ..." giờ hiện ngay dưới toolbar, trước "Giai đoạn lượt".

`tsc --noEmit` + `vitest run` (775 test) đều pass (chỉ sửa `ui.ts`/`style.css`, không đụng `core/`). **Chưa làm** (cần xác nhận kiến trúc trước theo đúng "Cách làm việc" — đổi state/protocol server): nút "Về phòng chờ (sửa tuỳ chọn)" trong Cài đặt của chủ phòng qua mạng — hiện "Bắt đầu ván mới" chỉ restart NGAY với tuỳ chọn CŨ (gửi `start_game` kèm `force:true`), không cho sửa house rules/expansions trước; cần thêm 1 `ClientMessage` mới (server xoá `GAME_STATE_KEY`, phát lại "lobby" cho cả phòng) — đã hỏi chủ dự án hướng làm, chờ xác nhận.

**Đổi chủ "dính màn hình khi cuộn" từ toolbar sang băng thông báo phản ứng (2026-09-09, cùng ngày)** — chủ dự án hỏi lại tại sao hàng nút toolbar bị "kéo theo" khi cuộn (đã giải thích: `.game-toolbar` dùng `position: sticky; top: 0` — cố ý từ đợt 3 UI/UX, thay cho `position: fixed` cũ từng đè lên seat trên cùng của bàn tròn) rồi CHỐT LẠI: muốn `.reaction-banner` (băng "⚠ Đang chờ...") có khả năng dính màn hình đó THAY VÌ toolbar — khẩn cấp cần phản hồi đáng dính hơn 4 nút Nhật ký/Thư viện/Cài đặt/Đầy đủ. Sửa: bỏ hẳn `position: sticky; top: 0; z-index: 5` khỏi `.game-toolbar` (giờ `position` mặc định static, cuộn trôi bình thường); thêm đúng 3 dòng đó (`position: sticky; top: 0; z-index: 5`) vào `.reaction-banner`, kèm đổi `margin: 0.75rem 0` → `margin: 0 0 0.75rem` (bỏ margin-top — nếu không sẽ hở 1 khoảng trong suốt phía trên khi dính, lộ nội dung cuộn qua bên dưới). Nhờ đợt sửa trước đã dời băng này lên NGAY SAU toolbar trong DOM (`renderPendingPanel()`/`networkRenderPendingPanel()`), khi cuộn qua khỏi toolbar thì băng là thứ tiếp theo "chạm" mép trên viewport và dính đúng lúc, không hở khoảng trống. Đã tự kiểm bằng trình duyệt thật (hotseat 4 người, có 1 việc đang chờ phản hồi thật — Kit Carlson xem 3 lá): cuộn xuống, toolbar biến mất khỏi màn hình như nội dung thường, băng "⚠ Đang chờ..." dính lại đúng mép trên; `getComputedStyle` xác nhận `toolbar.position === "static"`, `banner.position === "sticky"`, `banner.getBoundingClientRect().top === 0`. `tsc --noEmit` + `vitest run` (775 test) đều pass (chỉ sửa `style.css`, không đụng `ui.ts`/`core/`).

**Công tắc BẬT/TẮT sticky reaction-banner trong Cài đặt (2026-09-09, cùng ngày)** — chủ dự án muốn TẤT CẢ mọi người (không riêng chủ phòng) tự chọn được có muốn băng thông báo phản ứng dính màn hình (đợt sửa ngay trên) hay không. Đây là SỞ THÍCH CÁ NHÂN của người xem (giống `bang_theme`/`bang_font_size`/`bang_sound_enabled` — mỗi người 1 máy 1 lựa chọn riêng, đọc/ghi thẳng `localStorage`, KHÔNG phải luật chung cả bàn như house rules/expansions), nên không cần chờ chủ phòng, không cần đồng bộ qua server. Tách CSS: `.reaction-banner` giờ chỉ còn khung/màu (bỏ `position: sticky` ra khỏi đây), thêm class RIÊNG `.reaction-banner--sticky` chứa đúng `position: sticky; top: 0; z-index: 5`. `ui.ts` thêm `REACTION_BANNER_STICKY_STORAGE_KEY = "bang_reaction_banner_sticky"` + `isReactionBannerStickyEnabled()` (mặc định BẬT, cùng khuôn `isSoundEnabled()`/`isVibrationEnabled()`) — `renderPendingPanel()`/`networkRenderPendingPanel()` tự nối thêm class `--sticky` vào `panel.className` khi hàm này trả `true`. Thêm 1 `renderSwitchRow()` mới trong `renderSettingsDialogBody()` (ngay dưới công tắc "Rung khi tới lượt") — dùng CHUNG hàm này cho cả hotseat lẫn qua mạng (`renderSettingsDialogBody()` không tách 2 bản), nên "mọi người" ở cả 2 chế độ đều thấy nút y hệt, không cần sửa gì thêm để áp dụng cho qua mạng. Đã tự kiểm bằng trình duyệt thật (hotseat, có 1 việc đang chờ phản hồi thật): tắt công tắc → đóng dialog → banner mất `reaction-banner--sticky`, `getComputedStyle().position` đổi từ `"sticky"` sang `"static"` NGAY (không cần tải lại trang); bật lại → về đúng `"sticky"`. `tsc --noEmit` + `vitest run` (775 test) đều pass (chỉ sửa `ui.ts`/`style.css`, không đụng `core/`).

**Nút "Về phòng chờ (sửa tuỳ chọn)" — chủ phòng, chơi qua mạng (2026-09-09, cùng ngày) — ĐÃ CODE XONG, có hỏi xác nhận kiến trúc trước khi làm theo đúng "Cách làm việc"**: đây là việc còn thiếu duy nhất từ yêu cầu ban đầu (đợt "Thêm nút trong Cài đặt của chủ phòng — quay lại giao diện phòng chờ, cho phép sửa tuỳ chọn ván"). Khác hẳn "Bắt đầu ván mới" có sẵn (restart NGAY với house rules/expansions CŨ, gửi `start_game` kèm `force:true`) — nút mới HUỶ ván rồi đưa CẢ PHÒNG về màn hình lobby, KHÔNG tự tạo ván nào, chủ phòng tự sửa tuỳ chọn rồi tự bấm "Bắt đầu ván" lại khi sẵn sàng. **Phát hiện quan trọng lúc rà code trước khi làm**: hạ tầng cần thiết (xoá `GameState`, phát lại "lobby", chuyển màn hình client về lobby) ĐÃ CÓ SẴN GẦN NHƯ TOÀN BỘ từ việc 4.3 (cơ chế tự huỷ ván khi mất kết nối) — hàm `abandonGame()` (`room.ts`) đã làm đúng việc cần: xoá `GAME_STATE_KEY`/`DEADLINE_KEY`/`PAUSED_PLAY_KEY`/alarm rồi phát `ServerMessage` `game_abandoned`; client (`main.ts`'s `case "game_abandoned"`) đã reset toàn bộ state ván + chuyển `screen = "network-lobby"` sẵn. Chỉ cần: (1) `protocol.ts` — thêm `ClientMessage` mới `{ type: "return_to_lobby" }` + thêm field `reason: "disconnect" | "host_returned_to_lobby"` vào `game_abandoned` (trước đây chỉ có 1 lý do nên không cần field này) để client hiện đúng câu thông báo; (2) `room.ts` — thêm `handleReturnToLobby()` (kiểm `ownerId` giống hệt `handleStartGame()`, kiểm có ván đang chơi) rồi gọi THẲNG `abandonGame(undefined, "host_returned_to_lobby")` (không truyền `excludeSocket` — khác lúc gọi từ `webSocketClose()`, vì chính chủ phòng vừa gửi yêu cầu cũng cần nhận lại thông báo để chuyển màn hình); (3) `main.ts` — thêm `networkConfirmingReturnToLobby` (cùng khuôn `networkConfirmingNewGame`, reset ở đủ mọi điểm nó reset) + 3 hàm `onNetworkRequestReturnToLobby()`/`onNetworkConfirmReturnToLobby()`/`onNetworkCancelReturnToLobbyConfirm()`, và đổi câu thông báo trong `case "game_abandoned"` theo `message.reason`; (4) `ui.ts` — thêm interface `ReturnToLobbySettingsOptions` (tham số thứ 5, TUỲ CHỌN, của `renderSettingsDialogBody()` — hotseat KHÔNG truyền vì đã có tương đương qua `onPlayAgain()`), nút mới đặt cùng `.settings-actions` với "Bắt đầu ván mới" nhưng 2 bước xác nhận LOẠI TRỪ NHAU (bấm nút này thì ẩn nút kia, tránh 2 khối cảnh báo cùng hiện nếu bấm nhầm liên tiếp cả 2). Đã tự kiểm bằng trình duyệt thật (`wrangler dev`, 2 tab An+Bình, An tạo phòng nên là chủ phòng): xác nhận CHỈ An thấy nút "Về phòng chờ (sửa tuỳ chọn)" trong Cài đặt (Bình chỉ thấy "Rời phòng"); bấm nút → hiện đúng cảnh báo "sẽ HUỶ ván đang chơi", nút "Bắt đầu ván mới" tự ẩn đi (xác nhận cơ chế loại trừ hoạt động); bấm "Huỷ ván, về phòng chờ" → CẢ 2 tab đều tự chuyển về "Phòng chờ" với đúng câu "Chủ phòng đã quay về phòng chờ để sửa tuỳ chọn — ván trước đã bị huỷ"; An bấm "Bắt đầu ván" lại ngay sau đó thành công (xác nhận server không còn kẹt `GameState` cũ). Không lỗi console. `tsc --noEmit` + `vitest run` (775 test) đều pass (không đụng `core/`).

> **Lịch sử đầy đủ từng đợt làm việc** (rất dài — chi tiết từng quyết định, từng lần tự kiểm, từng đợt code) đã chuyển sang `CHANGELOG.md`. Đọc file đó khi cần biết TẠI SAO/THẾ NÀO một tính năng cụ thể đã được cài, hoặc còn thiếu gì ở 1 đợt cũ.

## Chưa làm tới, đừng đụng vào

Cả 3 biến thể số người chơi (2/3/8 người) đã HOÀN TẤT (xem 3 mục changelog "Biến thể số người chơi" trong CHANGELOG.md) — không còn biến thể nào đang dang dở.

Cơ chế "phát 2 lá nhân vật, chọn giữ 1" coi như HOÀN TẤT — core, UI (hotseat + qua mạng), và đồng hồ thật (15 giây, xem changelog sửa mốc thời gian trong CHANGELOG.md — ban đầu 30s, đã chỉnh lại) đều đã xong và ĐANG BẬT cho ván thật. Bia (Beer) cũng đã HOÀN THIỆN — cả ngoại lệ "vô tác dụng khi còn 2 người" lẫn cơ chế "hồi sinh tự động khi máu về 0". Việc 5.3 (house rules) đã có 4/6 luật nháp ban đầu, KÈM UI chọn thật (hotseat + qua mạng) — còn lại "gộp 2 lá Beer hồi máu người khác" (cần cơ chế chọn mục tiêu mới) để dành đợt sau. Còn lại ngoài ra: đồ hoạ đẹp (ảnh nhân vật thật trong `public/sprites/characters/`, giống ảnh lá bài — hiện vẫn ô xám vì chưa có file), tài khoản/đăng nhập, bảng xếp hạng. **Âm thanh**: khung Cài đặt (toggle bật/tắt + `playSound()`) đã xong, nhưng CHƯA có file `.mp3` thật trong `public/sounds/` VÀ CHƯA gắn vào sự kiện ván đấu nào (đánh bài, mất máu...) — để dành đợt sau, giống nếp "ảnh tới đâu gắn tới đó".

**BUG đã sửa (phát hiện 2026-08-06, sửa cùng ngày)**: `cardActsAsMissed()` ở `ui.ts` thiếu 2 nhánh so với `actsAsMissed()` thật ở `reduce.ts` — (1) Elena Fuente (`hasAnyCardMissedAlias`, mở rộng Dodge City): bất kỳ lá nào trên tay cũng dùng được như Missed!; (2) lá "dodge" (Dodge City) tự thân cũng đóng vai Missed! (khác tên "missed" nên bị bỏ sót). Cả 2 nhánh giờ đã nối vào `cardActsAsMissed()`, mirror đúng `reduce.ts`. Đã chạy `tsc --noEmit` + `vitest run` (441 test) đều pass sau khi sửa — CHƯA tự kiểm bằng trình duyệt thật (Elena Fuente/lá Dodge hiếm gặp lúc random).

**5.4 (Dodge City) — CORE + UI COI NHƯ HOÀN TẤT** (xem changelog "Mở rộng Dodge City — đợt 1"/"đợt 2"/"đợt 3"/"Vera Custer" trong CHANGELOG.md): kiến trúc trang bị trì hoãn (mục A) + toàn bộ 40 lá bài mới (mục B) + luật riêng biến thể 3 người (mục D) + cơ chế "bỏ kèm 1 lá phụ"/Brawl (mục E) + UI hiển thị chất bài (mục F) + 15/15 nhân vật (mục C) đều xong core+test từ trước. Đợt UI vừa xong (đã tự kiểm bằng trình duyệt thật, hotseat 8 người):

1. **Kích hoạt lá vàng "trì hoãn" đã bày sẵn** (Derringer/Knife/Pepperbox/Buffalo Rifle/Conestoga/Can Can cần chọn mục tiêu — tái dùng ĐÚNG luồng "picking-target"/"picking-panic-equipment"/"picking-cat-balou-zone" đã có sẵn cho Bang!/Panic!/Cat Balou; Canteen/Pony Express/Howitzer không cần mục tiêu) — bấm vào lá trên sân gửi `PLAY_CARD`, `reduce()` tự nhận biết đây là kích hoạt qua `activateDelayedEquipment()` có sẵn.
2. **Đáp lại NEED_MISSED bằng lá vàng trên sân** (Bible/Sombrero/Ten Gallon Hat/Iron Plate, đã qua ≥1 lượt) — gửi `RESPOND` kèm `cardId`, y hệt Missed! trên tay. Phải lộ thêm `equipmentPlayedTurn`/`turnNumber` qua `PlayerView` (`view.ts`) — không bí mật gì (lá bày ngửa công khai).
3. **Lá cần bỏ kèm 1 lá phụ** (Brawl/Rag Time/Springfield/Tequila/Whisky) — Selection thêm 2 bước mới `"picking-brawl-zones"` (Brawl: người đánh chọn vùng tay/sân riêng cho TỪNG nạn nhân trước khi gửi) và `"picking-extra-discard"` (bước CUỐI của cả 5 lá, gom đủ targetId/targetCardId/brawlZones rồi mới hỏi lá phụ). Tequila cho tự chọn CHÍNH MÌNH làm mục tiêu (ngoại lệ duy nhất). Tiện thể sửa luôn 1 bug cũ không liên quan Dodge City: **Punch thiếu hẳn bước chọn mục tiêu** (thiếu trong `NEEDS_TARGET`, bấm là lỗi ngay) — đã thêm vào cùng chỗ.
4. **`USE_ABILITY`** (Sid Ketchum/Chuck Wengam/José Delgado/Doc Holyday) — nút "Dùng kỹ năng" hiện theo ĐÚNG điều kiện từng người (Sid Ketchum: BẤT CỨ LÚC NÀO, không cần lượt/không pending; 3 người còn lại: chỉ lượt chính mình, không pending, đúng giới hạn số lần/lượt). Cần chọn đủ số lá (Selection `"picking-ability-cards"`) trước khi gửi — Doc Holyday còn thêm bước chọn mục tiêu (`"picking-ability-target"`). Phải lộ thêm `joseDelgadoUsesThisTurn`/`docHolydayUsedThisTurn` qua `PlayerView`.
5. **Pat Brennan** (`NEED_PICK_DRAW_OR_EQUIPMENT`) — nút "Rút bộ bài" + danh sách trang bị bấm được của từng người khác.
6. **Vera Custer** (`NEED_PICK_BORROWED_CHARACTER`) — nút chọn mượn khả năng của từng người khác đã có nhân vật.
7. Mô tả kỹ năng (tooltip) cho đủ 15/15 nhân vật Dodge City ở màn chọn nhân vật (trước đó chỉ có 16 nhân vật gốc).

**Đã tự kiểm bằng trình duyệt thật** (không chỉ đọc code): kích hoạt Derringer (có mục tiêu), đỡ Bang! bằng Mũ Sombrero, dùng kỹ năng Sid Ketchum (bất cứ lúc nào), và ĐỦ CẢ 5 lá bỏ-kèm (Whisky/Brawl/Rag Time/Springfield/Tequila, kể cả Tequila tự chọn mình) — tất cả đúng như thiết kế, không lỗi console. **Chưa tự kiểm bằng trình duyệt** (chỉ code review, do random draw hiếm gặp/chưa random ra đúng nhân vật): Punch, USE_ABILITY của Chuck Wengam/José Delgado/Doc Holyday — cùng cơ chế/luồng đã kiểm ở trên, rủi ro thấp.

Bộ mở rộng "dodge_city" (tách riêng khỏi house rules cũ, xem `ExpansionId` ở `types.ts`) khi bật cộng đủ 40/40 lá vào bộ bài VÀ đưa 15 nhân vật Dodge City vào bộ bốc "phát 2 lá, chọn giữ 1" — kiến trúc đã sẵn cho nhiều bộ mở rộng cùng lúc (mảng `expansions`), dù hiện chỉ có 1 bộ. **Chưa quyết định deploy** (kể cả bản beta) — để chủ dự án tự quyết, chỉ còn thiếu ảnh nhân vật/lá bài thật (cosmetic, không chặn chơi được).

**Mục C (15/15 nhân vật) HOÀN TẤT** — nhóm A (7 người, dùng lại cơ chế có sẵn): Pixie Pete/Bill Noface (`onDrawPhase`), Greg Digger/Herb Hunter (`onAnyDeath`), Pat Brennan (hook mới `canTakeEquipmentInsteadOfDraw` + `PendingAction` mới `NEED_PICK_DRAW_OR_EQUIPMENT`), Chuck Wengam (`canPayLifeToDraw`) + José Delgado (`canDiscardEquipmentToDraw`, thêm `GameState.joseDelgadoUsesThisTurn`) — 2 người sau dùng CHUNG action `USE_ABILITY` với Sid Ketchum nhưng CHỈ trong lượt chính mình. Nhóm B (4 người, hook mới nhưng độc lập): Sean Mallory (`modifyHandLimit`), Tequila Joe (`modifyHealAmount` chỉ ở `playBeer()` + `doubleRevivalHp` riêng cho hồi sinh tự động), Elena Fuente (`hasAnyCardMissedAlias` + `canUseOwnEquipmentAsMissed`), Apache Kid (`isImmuneToCard` — miễn nhiễm lá chất Rô nhắm vào mình, KHÔNG áp dụng Duel/Indians!). Nhóm C (3 người, phụ thuộc lẫn nhau): Molly Stark (`onVoluntaryPlayOutOfTurn`, ngữ cảnh "immediate"/"duel" — `GameState.duelBangDrawPending` dồn Bang! trong Duel, chỉ rút khi Duel thật sự kết thúc), Doc Holyday (`canDiscardTwoForBang`, dùng chung `USE_ABILITY`, thêm `targetId?` vào action + `GameState.docHolydayUsedThisTurn`; miễn nhiễm Apache Kid chỉ khi CẢ 2 lá chất Rô), Belle Star (`disablesOthersEquipment`, hàm trung tâm `getEffectiveEquipment()`). **Vera Custer (nhân vật cuối cùng)** — cơ chế uỷ quyền toàn hệ thống hook: field tĩnh `canBorrowCharacterAbilities` + 3 hàm TRUNG TÂM mới `getEffectiveCharacterId()`/`getEffectiveCharacterHooks()`/`getEffectiveCharacterDefinition()` ở `characters.ts` — MỌI lời gọi `getCharacterHooks()`/`getCharacterDefinition()` rải rác trong `core/` (~30 điểm, đã rà soát trước khi code) đều đổi sang tra qua 3 hàm này. `GameState.veraCusterBorrowedCharacterId` (characterId đang mượn) + `PendingAction` mới `NEED_PICK_BORROWED_CHARACTER` — đẩy Ở BƯỚC ĐẦU TIÊN của lượt cô ta, TRƯỚC CẢ draw!-check Dynamite/Jail (đã hỏi lại và chốt). Mượn được TẤT CẢ nhân vật, không ngoại lệ — CHỈ mượn hook/field tĩnh, KHÔNG mượn bullets/maxHp.

**LƯU Ý (ĐÃ SỬA)**: `dealCharacterCards: true` vẫn hardcode bật thật trong `room.ts`, nhưng 15 nhân vật Dodge City giờ CHỈ vào bộ bốc khi chủ phòng tick bộ mở rộng "dodge_city" (checkbox riêng trên màn hình thiết lập, xem `EXPANSION_CHARACTER_IDS` ở `characters.ts` + lọc ở `setup.ts`) — ván thật KHÔNG tick thì KHÔNG bao giờ phát nhầm 1 trong 15 nhân vật này nữa. UI cho kỹ năng chủ động của Pat Brennan/Chuck Wengam/José Delgado/Doc Holyday/Vera Custer đã xong (xem mục 5.4 phía trên) — không còn nhân vật nào "vô hiệu" một phần nữa.

Cả 16 nhân vật gốc + 15 nhân vật Dodge City đều đã có THẬT trong `core/characters.ts` VÀ đã có nút bấm thật trên giao diện (hotseat lẫn qua mạng, kèm đồng hồ hết giờ tự động) — bao gồm cả `USE_ABILITY` (Sid Ketchum/Chuck Wengam/José Delgado/Doc Holyday, xong ở đợt UI lá vàng mục 5.4 phía trên — trước đó có khoảng thời gian ngắn 0 nút bấm nào dùng action này, kể cả Sid Ketchum, phát hiện lúc rà UI Dodge City 2026-08-06, đã sửa ngay).
